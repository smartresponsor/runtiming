# FrankenPHP worker mode (Symfony Runtime)

# NOTE: Dotenv runs after Runtime, so APP_RUNTIME should be exported by your runner/container.
APP_RUNTIME=Runtime\FrankenPhpSymfony\Runtime

# FrankenPHP worker mode script (boot once, serve many).
# For Docker: FRANKENPHP_CONFIG="worker ./public/index.php"
FRANKENPHP_CONFIG=worker ./public/index.php

# Optional safety: restart worker after N requests (default runtime package = 500).
# frankenphp_loop_max=500
