# Copyright (c) 2025 Oleksandr Tishchenko / Marketing America Corp
# runtime-supercharger-gate-target.yaml
# Informational: examples for RUNTIME_GATE_ROUTE_LIST sets.

read_default:
  route:
    - /status
    - /api/category/resolve?slug=test
  threshold:
    p95_ms_max: 250
    fail_rate_max: 0.005

write_default:
  route:
    - /api/order/create
    - /api/message/send
  threshold:
    p95_ms_max: 700
    fail_rate_max: 0.005
